# Task 1: Fix getAvailableCapacity() - Evidence

## Implementation Completed
File: src/modules/tour/service.ts (lines 50-79)

## Changes Made
✅ Replaced `return tour.max_capacity - bookings.length` with passenger counting logic
✅ Properly parses nested structure: booking.line_items.items[].metadata.passengers
✅ Sums adults + children only (infants excluded from capacity)
✅ Defensive handling: null/undefined line_items → 0 passengers
✅ Defensive parsing: Number(passengers.adults) || 0 to handle non-numeric values

## New Logic
```typescript
const reservedPassengers = bookings.reduce((total, booking) => {
  // Handle potential missing line_items defensively
  const lineItemsData = booking.line_items as { items?: Array<{ variant_id: string, metadata?: { passengers?: { adults?: number, children?: number, infants?: number } } }> } | null | undefined
  const items = lineItemsData?.items || []
  
  const bookingPassengers = items.reduce((bookingTotal, item) => {
    const passengers = item.metadata?.passengers || {}
    const adults = Number(passengers.adults) || 0
    const children = Number(passengers.children) || 0
    // Infants are EXCLUDED from capacity
    return bookingTotal + adults + children
  }, 0)
  
  return total + bookingPassengers
}, 0)

return tour.max_capacity - reservedPassengers
```

## TypeScript Verification
Build completed successfully (npm run build):
- Backend build: ✅ Completed (pre-existing errors in other files unrelated to this change)
- Frontend build: ✅ Completed successfully
- No NEW errors introduced by this change

## Edge Cases Handled
1. null/undefined line_items → defaults to empty array → 0 passengers
2. Missing metadata → defaults to empty object → 0 passengers  
3. Non-numeric passenger values → Number() conversion with || 0 fallback
4. Multiple line items per booking → correctly sums across all items
5. Infants explicitly excluded from capacity calculation

## Testing Recommendation
Test with bookings containing:
- Multiple line items (adults + children in separate items)
- Edge cases: null line_items, missing metadata
- Infants in passenger counts (should NOT reduce capacity)
